@testable import SharedA
import XCTest

final class SharedATests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SharedA().text, "Hello, World!")
    }
}
